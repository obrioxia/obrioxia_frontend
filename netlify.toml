[build]
  command = "npm install && npm run build"
  # CRITICAL FIX: Point to the 'browser' subfolder
  publish = "dist/obrioxia-frontend/browser"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20"
