[build]
  # Force legacy peer deps to ignore any remaining version warnings
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist/obrioxia-frontend/browser"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20"
