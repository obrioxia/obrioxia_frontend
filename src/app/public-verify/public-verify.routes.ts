import { Routes } from '@angular/router';
import { PublicVerifyComponent } from './public-verify.component';

export const PUBLIC_VERIFY_ROUTES: Routes = [
  { path: '', component: PublicVerifyComponent }
];


